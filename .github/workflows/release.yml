name: Release

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
      - "rc-[0-9]+.[0-9]+.[0-9]+"
      - "alpha-[0-9]+.[0-9]+.[0-9]+"
      - "beta-[0-9]+.[0-9]+.[0-9]+"

permissions:
  contents: write     # create release, upload assets
  id-token: write     # cosign keyless signing

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9f698171ed81b15d1823a05fc7211befd50c8ae0 # v6.0.3

      - uses: pnpm/action-setup@d15e628ca66d93ee5f352c71671a7bc6a97af5c9 # v6.0.8

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version-file: ".nvmrc"
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Compute release metadata
        id: meta
        run: |
          set -eu
          TAG="${GITHUB_REF#refs/tags/}"
          # Strip the `v` or `<prefix>-` from tag to get the bare version.
          case "$TAG" in
            v*)         VERSION="${TAG#v}" ;;
            *-*)        VERSION="${TAG#*-}" ;;
          esac
          REL="https://github.com/${GITHUB_REPOSITORY}/releases"
          MANIFEST="${REL}/latest/download/system.json"
          DOWNLOAD="${REL}/download/${TAG}/nonex-ist-od6s.zip"
          {
            echo "tag=$TAG"
            echo "version=$VERSION"
            echo "manifest=$MANIFEST"
            echo "download=$DOWNLOAD"
            # Tags starting with `v` are stable releases; everything else
            # (rc-/alpha-/beta-) is a prerelease and must not become "latest".
            if [ "${TAG#v}" != "$TAG" ]; then
              echo "prerelease=false"
            else
              echo "prerelease=true"
            fi
          } >> "$GITHUB_OUTPUT"
          sed -i "s|\"version\":.*|\"version\": \"${VERSION}\",|" src/system.json
          sed -i "s|\"manifest\":.*|\"manifest\": \"${MANIFEST}\",|" src/system.json
          sed -i "s|\"download\":.*|\"download\": \"${DOWNLOAD}\",|" src/system.json

      - run: pnpm run build

      - name: Package zip
        run: |
          set -eu
          mkdir -p build/artifacts
          cp -r src build/nonex-ist-od6s
          (cd build && zip -r artifacts/nonex-ist-od6s.zip nonex-ist-od6s/)
          (cd build/artifacts && sha256sum nonex-ist-od6s.zip > nonex-ist-od6s.zip.sha256)

      - name: Generate CycloneDX SBOM
        run: >
          pnpm exec cdxgen
          --type pnpm
          --no-recurse
          --spec-version 1.6
          --output build/artifacts/sbom.cdx.json
          .

      - name: Extract CHANGELOG section
        id: changelog
        run: |
          set -eu
          VERSION="${{ steps.meta.outputs.version }}"
          TAG="${{ steps.meta.outputs.tag }}"
          # Pull the section bounded by `## [VERSION]` and the next `## [`,
          # then reflow paragraphs and list items so hard wraps in the
          # source CHANGELOG don't render as visible <br>s in release
          # notes (GitHub Releases honor GFM `breaks: true`).
          # Reflow rules:
          #   - blank lines and headers (`#`) flush + pass through
          #   - lines starting `- ` / `* ` start a new buffered list item
          #   - any other non-empty line appends to the current buffer,
          #     joined with a single space (leading indent stripped)
          # If the version isn't found (e.g. someone tagged without
          # updating CHANGELOG), fall back to a bare link.
          # `index($0, header) == 1` is a literal-substring check — avoids
          # treating the dots in `VERSION` as regex metacharacters.
          SECTION=$(awk -v header="## [$VERSION]" '
            BEGIN              { capture=0; buf="" }
            function flush()   { if (buf != "") { print buf; buf="" } }
            index($0, header) == 1 { capture=1; next }
            capture && /^## \[/    { flush(); exit }
            !capture           { next }
            /^$/               { flush(); print ""; next }
            /^#+ /             { flush(); print; next }
            /^[-*] /           { flush(); buf=$0; next }
                               { line=$0; sub(/^[ \t]+/, "", line);
                                 buf = (buf == "" ? line : buf " " line) }
            END                { flush() }
          ' CHANGELOG.md)
          CHANGELOG_URL="https://github.com/${GITHUB_REPOSITORY}/blob/${TAG}/CHANGELOG.md"
          {
            if [ -n "$SECTION" ]; then
              printf '%s\n' "$SECTION"
              echo
              echo "[Full changelog →](${CHANGELOG_URL})"
            else
              echo "[Release notes in CHANGELOG.md →](${CHANGELOG_URL})"
            fi
          } > release-body.md
          echo "body_path=release-body.md" >> "$GITHUB_OUTPUT"

      - uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2

      - name: Sign artifacts (cosign keyless)
        env:
          COSIGN_YES: "true"
        run: |
          set -eu
          cd build/artifacts
          cosign sign-blob --bundle nonex-ist-od6s.zip.bundle           nonex-ist-od6s.zip
          cosign sign-blob --bundle sbom.cdx.json.bundle      sbom.cdx.json
          cd "$GITHUB_WORKSPACE"
          cosign sign-blob --bundle src/system.json.bundle    src/system.json

      - name: Create GitHub Release
        uses: softprops/action-gh-release@b4309332981a82ec1c5618f44dd2e27cc8bfbfda # v3
        with:
          tag_name: ${{ steps.meta.outputs.tag }}
          prerelease: ${{ steps.meta.outputs.prerelease }}
          # `body_path` is prepended; `generate_release_notes: true`
          # then appends GitHub's auto-generated "What's Changed" PR list.
          body_path: ${{ steps.changelog.outputs.body_path }}
          generate_release_notes: true
          files: |
            build/artifacts/nonex-ist-od6s.zip
            build/artifacts/nonex-ist-od6s.zip.sha256
            build/artifacts/nonex-ist-od6s.zip.bundle
            build/artifacts/sbom.cdx.json
            build/artifacts/sbom.cdx.json.bundle
            src/system.json
            src/system.json.bundle
